// Alias route so both /advisor and /ai-advisor point to the same
// Nova command center — keeps existing internal links working while
// exposing the more branded "AI advisor" URL requested in the redesign PRD.
export { default } from '@/app/advisor/page'
